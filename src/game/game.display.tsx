import * as React from 'react';
import { Box, Flex } from '../design-system/atoms';

const GameOfColorsDisplay = () => {
	return (
		<Flex width={200}>
			<Box display="block" background="red" />
		</Flex>
	);
};

export default GameOfColorsDisplay;
